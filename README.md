# API-Gateway-Email-Automation
n end-to-end serverless automation system that processes API Gateway change requests sent via email. Developers send a structured email, and the system automatically parses it using AI, patches the OpenAPI template, and raises a Pull Request on BitBucket — with no manual JSON editing required.
